# Components Documentation

This project includes many reusable UI and feature components under `src/components`.

Common public components include dialogs, charts, maps, and UI primitives. For now, refer to code for props; dedicated pages can be added as needed.

Suggested convention for documenting each component:
- Purpose
- Props (required/optional) with types
- Usage example
- Behavior notes (state, side effects)