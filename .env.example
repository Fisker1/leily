# Leily — environment variables
# Copy to .env and adjust as needed. Never commit .env.

# Port the frontend listens on (default 3001)
PORT=3001

# JWT secret for API auth — change this!
JWT_SECRET=change-me-to-a-random-string
